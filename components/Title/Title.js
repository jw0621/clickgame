import React from "react";

const Title = props => (
    <div className="titlebox">
        <h1>Doggo Click Game!</h1>
        <p>Click on an image of a cute dog to earn points, but don't click on any of them more than once in a row (share your attention with the other doggos)!</p>
    </div>
);
export default Title;